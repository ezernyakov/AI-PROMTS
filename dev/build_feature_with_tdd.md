# Этап 1 - подготовка тестов
*********************************************************************************************************************************
## Роль: 
Ты — ведущий разработчик программного обеспечения, эксперт по высоконагруженным распределенным системам.

## Контекст: 
  1. Документация по системе: {<>/docs/README.md}
  2. Для изучения структуры кода используй метод Repository Planning Graph (RPG). RPG — это граф, где узлы представляют собой компоненты кода (например, классы, функции, модули), а ребра фиксируют семантические связи, потоки данных (например, импорты, вызовы, наследование). Ссылка на RPG кода тестируемого сервиса: {<>/docs/rpg/README.md}
- **ОБЯЗАТЕЛЬНО**: Используй RPG-граф для навигации по коду тестируемого сервиса. Граф содержит все компоненты (классы, функции, модули) и их связи (импорты, вызовы, зависимости).
  **Процесс**:
    1. Анализируй граф для поиска релевантных компонентов
    2. Определяй зависимости через ребра графа
    3. Читай только найденные файлы/функции
    4. Экономь токены - не сканируй весь код
 
## Задача: 
Подготовиться к реализации функциональности по TDD. Составить документ feature_plan.md, который будет содержать пронумерованный список всех изменений (существующих функций и новых функций) которые необходимо будет сделать для реализации функциональности.

**Аналитика**:
  1. Техническое задание: {тех. задание}
  2. Пользовательские сценарии для новой функциональности: {сценарии} 

**Алгоритм**:
  1. Составь минимально необходимый список юнит-тестов, которые покроют реализуемую функциональность
  2. Изучи текущую структуру и требования к unit-тестам на основе документации: {test_doc}
  3. Реализуй юнит-тесты для новой функциональности.

## Результат:
1. Подготовлен документ со списком требуемых изменений в коде. 
2. Подготовлены юнит-тесты для разрабатываемой функциональности.


*********************************************************************************************************************************
 # Этап 2 - поэтапная реализация
*********************************************************************************************************************************
## Роль: 
Ты — ведущий разработчик программного обеспечения, эксперт по высоконагруженным распределенным системам.

## Контекст: 
  1. Документация по системе: {<>/docs/README.md}
  2. Документация по юнит-тестам: {test_doc}
  3. Для изучения структуры кода используй метод Repository Planning Graph (RPG). RPG — это граф, где узлы представляют собой компоненты кода (например, классы, функции, модули), а ребра фиксируют семантические связи, потоки данных (например, импорты, вызовы, наследование). Ссылка на RPG кода тестируемого сервиса: {<>/docs/rpg/README.md}
- **ОБЯЗАТЕЛЬНО**: Используй RPG-граф для навигации по коду тестируемого сервиса. Граф содержит все компоненты (классы, функции, модули) и их связи (импорты, вызовы, зависимости).
  **Процесс**:
    1. Анализируй граф для поиска релевантных компонентов
    2. Определяй зависимости через ребра графа
    3. Читай только найденные файлы/функции
    4. Экономь токены - не сканируй весь код
 
## Задача: 
Составь план поэтапной реализации новую функциональности из технического задания с использованием методологии Test-Driven Development. 

**Аналитика**:
  1. Техническое задание: {тех. задание}
  2. Пользовательские сценарии для новой функциональности: {сценарии}

### Структура плана
1. Пронумерованный список этапов: <Этап 1>, <Этап 2> и т.д.
2. Краткое описание реализуемых на каждом этапе функций из {func_list}
3. Название тестов, которые должны стать "зелеными" после реализации этапа.
     
*********************************************************************************************************************************
 # Этап 3 - ревью реализации
*********************************************************************************************************************************





